/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: true,
  },
  images: {
    domains: ["qujgsyngpismiyphgkip.supabase.co"],
  },
};

module.exports = nextConfig; 